name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Test
        run: make test
      - name: Build
        run: make
      - name: Verify signature
        run: codesign --verify --deep --strict camlight.app
